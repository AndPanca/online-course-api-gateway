const create = require('./create');
const getAll = require('./getAll');
const destroy = require('./destroy');

// Identifikasi semua service media handler
module.exports = {
  create,
  getAll,
  destroy
};